import React from 'react';

const DeleteModal = () => {
    return (
        <div>
            //modal with question Are you shure you want to delete task?
            //what task
            //1) btn -> delete
            //2) btn -> cancel
        </div>
    );
};

export default DeleteModal;
